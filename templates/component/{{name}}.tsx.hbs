import React, { ReactElement, HTMLProps } from 'react';
import * as Styles from './{{name}}.styles';
import { {{name}}Props } from './{{name}}.types';



const {{name}} = (props: {{ name }}Props) => {
return (
<Styles.Container>{{name}}</Styles.Container>
);
}

export default {{name}};